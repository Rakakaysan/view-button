import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  button: {
    backgroundColor: "#e1ff00ff",
    padding: 16,
    borderRadius: 20,
  },
  buttonText: {
    fontSize: 16,
    color: "#000000ff",
    textAlign: "center",
  },
});
